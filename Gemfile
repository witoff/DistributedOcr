# A sample Gemfile
source "https://rubygems.org"

# Ensure we're using the right version.  2 is not yet released
#  and not backwards compatibile
gem 'aws-sdk', '<2'

gem 'aws-flow'

# Fix the ruby #
#ruby '2.1.1'
